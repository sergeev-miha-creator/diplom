# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api.spec.js >> API challenge >> GET /todos (200)
- Location: tests/api.spec.js:15:9

# Error details

```
ReferenceError: ChallengesService is not defined
```

# Test source

```ts
  1  | import { ChallengerService, TodosService } from './index';
  2  | 
  3  | export class ApiClient {
  4  |     constructor (options) {
  5  |         const defaultOptions = {
  6  |             URL: "https://apichallenges.eviltester.com/",
  7  |         }
  8  |         const mergeOptions = {
  9  |             ...defaultOptions,
  10 |             ...options,
  11 |         }
  12 |         this.challenger = new ChallengerService(mergeOptions);
> 13 |         this.challenges = new ChallengesService(mergeOptions);
     |                               ^ ReferenceError: ChallengesService is not defined
  14 |         this.todos = new TodosService(mergeOptions);
  15 |         this.todo = new TodoService(mergeOptions);
  16 |     };
  17 | 
  18 |     static async loginAs(){
  19 |         const client = this.unauthorized();
  20 |         //Авторизация
  21 | 
  22 |         const { headers } = await client.challenger.post();
  23 |         const token = headers["x-challenger"]
  24 | 
  25 | 
  26 |         return new ApiClient({token});
  27 |     }
  28 | 
  29 |     static unauthorized(){
  30 |         return new ApiClient();
  31 |     }
  32 | }
```