{
  "name": "diplom",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "allureGenerate": "allure generate --clean",
    "allureOpen": "allure open",
    "APItest": "npx playwright test --grep '@API'",
    "UItest": "npx playwright test --grep '@UI'",
    "api2": "export NODE_ENV=dev : export ENVIRONMENT=staging : npx playwright test --grep @API",
    "api13": "export ENVIRONMENT=staging && npx playwright test --grep @API",
    "debug": "npx playwright test --ui",
    "test": "npx playwright test",
    "reportAwesome": "npx allure awesome ./allure-results --single-file",
    "reportClassic": "npx allure classic ./allure-results --single-file",
    "report": "allure generate ./allure-results -o ./allure-report : allure open ./allure-report"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sergeev-miha-creator/diplom.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^10.4.0",
    "@playwright/test": "^1.60.0",
    "@types/allure-js-commons": "^0.0.4",
    "@types/node": "^25.9.0",
    "allure-playwright": "^3.9.0",
    "cross-env": "^7.0.3",
    "dotenv": "^17.4.2",
    "rimraf": "^6.1.3"
    
  }
}
